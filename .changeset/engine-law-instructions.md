---
"@chbrain/khai-cultures": patch
---

Declare the engine law (governance step from the language-engine design of record). The house gains its first `instructions` instance, management/canon_instructions.md, a HACKS card (Human, Agent, Collaboration, Knowledge, System) whose Knowledge chapter declares the language-engine law and links the root process_using_language.md, so the ladder reads system-wide: every persona reads per language, per channel, at its true width, and the prose enacts the gap where a culture is lost at the limits of a tongue. It also fixes the rollout cadence (one culture per PR, sequential), the casting law, and the Estate, version, and protection rules in one place. Non-content governance; the culture count is unchanged.
