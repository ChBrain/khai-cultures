---
---

Take khai-guard 0.2.1 and make the identifier a hard gate again. The guard now judges the count-driven add rule on a rename's source as well as its destination, so a play that moved is no longer read as a play that arrived, whether or not the move carried edits. That was the one blocker: with it fixed, a sub-national culture can be renamed in the same pull request as the content it belongs to, and the id half of conformance stops being a notice and starts failing again. Verified against the guard by renaming a culture directory both ways and watching the add finding disappear. Governance only; the house ships nothing.
