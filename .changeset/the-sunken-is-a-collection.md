---
---

**The readers for a third collection, and the glob that makes it republish.**

The sunken gets the same three readers groups have, the same entry builders, and
a place in `countDrivenAdd`. No content moves and the registry is byte-identical:
the collection itself is declared in the pull request that follows.
