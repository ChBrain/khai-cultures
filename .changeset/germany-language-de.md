---
"@chbrain/khai-cultures": patch
---

Germany: declare `language: de` on the play (per-play language, now that the engine enforces the language policy) and trim `geo.json` to iso-only (the website derives region/parent from the ISO code).
