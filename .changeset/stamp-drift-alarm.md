---
---

Wire the khai-drift alarm, and stamp the management README. This house had no
way to tell it was behind the kit: no drift workflow, no `driftPolicy`, and a
dependabot that was still trying (and failing) to read `@chbrain/*` from GitHub
Packages. All three now match what khai-stage stamps into a house, so the
weekly run reports kit drift into a single issue rather than nobody noticing.
Tooling only: nothing here is under the package's published `files`.
