---
---

The house becomes a package, so the root can be the workspace container khai’s
root is.

Empty, and the guard is right that it should be. A relocation ships no content:
the name and the version are unchanged, and `changeset version` bumping from a
manifest that has just appeared at a new path would ship one above the version
the registry already has.
