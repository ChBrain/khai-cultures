---
---

Record the canon answer and what the workspace attempt found. khai opened the production layer: a package may ship one khai play, declaring `class: "house"` (the canon class of a play, not a new word), a production id, no `khai.engine`, exactly one anchored play, and no markdown link escaping the package with `../`. Two corrections follow. The house card does not change, because a card's package is what the website pulls and the umbrella's dependencies already are the list of its productions. And khai-pack is not the mechanism: it ships bytes, while the split exists for a reference npm can check. The house publish gate now delegates the canon, the language policy and the `../` invariant to `validateProductionPackage` rather than reimplementing them. Separately, the workspaces skeleton was tried and reverted within the hour: adding a workspaces field puts changesets into monorepo mode, where the root package is not a member, which invalidates every pending changeset and would have broken the open release. The repository can be a published package or a workspace root, not both, so step 5 is now to drain the release first and then move the umbrella into the workspace in one atomic commit. Governance only; the house ships nothing.
