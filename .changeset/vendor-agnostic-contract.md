---
---

The house converges on the kit's vendor-agnostic contract layout: `AGENTS.md` at
the root now carries the coding contract, `house_instructions.md` moved into it
rather than being duplicated, and `CLAUDE.md`, `GEMINI.md`, `PERPLEXITY.md` and
`.github/copilot-instructions.md` are thin pointers carrying only their own
tool's quirks. `management_instructions.md` and `position_playwright.md` are
now byte-identical to the blueprint.

Ships nothing: governance and docs only.
