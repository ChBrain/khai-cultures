---
---

Take khai-language 0.1.25, which derives the scanned chapter set from khai-arch
instead of a hand-typed list of fifteen names. That list left 612,572 words, 43% of
the house's prose, unread: every chapter of every pitch and every process, `Has` and
`Drives` on every position, `Cue` and `Tension` on every plot. Thirty-two chapters
are read now, and the four excluded are all identity or cast lists on `play`, `Estate`
among them, which was the one thing that could have broken this house — every
non-English culture's README carries the house's own English line there, and the gate
skips READMEs entirely anyway. Zero new findings across the house, which is the same
answer the pre-flight gave before the version existed. What did break is the clock:
the language policy test was capped at 30 seconds, set when the gate read less than
half of this, and the check now measures 36.9 seconds over 6,171 files with sixteen
more languages actually being detected since the exempt list was trimmed. The cap
goes to 120 seconds with the measurement written beside it. Governance only; the
house ships nothing.
