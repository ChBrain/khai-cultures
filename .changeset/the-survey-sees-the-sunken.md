---
---

**The `next` survey counted zero sunken units while one sat in the umbrella.**

`sunkenUnits()` walked `packages/*` for a manifest declaring `khai.sunken` and
nothing else, so it found no unit authored in the umbrella - which is where every
unit in this house starts. It reads both homes now, the way the group half of the
survey already did, and `migrated` is read rather than hardcoded.
