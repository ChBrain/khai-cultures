---
---

**Wire the kit's own walls into the house's suite, and read one of them off
against the house's own reasoning for why it does not carry it.**

`@chbrain/khai-tests` 0.4.2 ships instruments this house used to run only by
hand, if at all: the gates manifest held against the CI workflow's own job
ids, the release workflow held to the changesets v2 input names, registry.json's
promise held against the packed tarball, the management blueprint convergence,
and the two house content walls, a cross-unit link that stays inside its own
unit and a filename that stays ASCII. `khai-guard.config.json` now carries
`ciPolicy.split` for the one CI job that runs prettier and conformance as two
steps, and `isolationPolicy.allow` for the two link idioms this house already
writes on purpose: a child unit citing a parent's culture position, and a
persona citing a tongue's language position. Both were red until the policy
said so, which is the whole point of a wall that starts red on content a house
wrote deliberately.

One instrument was tried and set aside on purpose: `verifyRegistry` builds
from the collection directory alone, so on this hybrid house it reads every
migrated culture as a missing directory and the registry as stale because of
it, the exact shape `tests/registry_hybrid.mjs` was written to replace rather
than filter. Wiring it in would fail the suite on the house's own migration,
not on a real fault, so `tests/registry_hybrid.mjs`'s own drift check stays
the wall, unchanged.

Ships nothing.
