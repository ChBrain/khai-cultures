---
---

Teach the guard about `packages/`. The walk publishes the language varieties from a package beside the umbrella, so the culture lane that carries the walk must be allowed to touch it, and the source/test separation must count it as source rather than as neither. Two lines of configuration ahead of the first slice; no content moves here. Governance only; the house ships nothing.
