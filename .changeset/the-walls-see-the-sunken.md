---
---

**Three walls could not see the sunken collection, and now can.**

`units()` was the cultures plus the groups, so `links`, `link-names` and
`type-titles` reported "no unit written" about the thirteen files of the first
sunken play rather than refusing them by name. One reader feeds all three, so one
line fixes all three. A test plants a violation in the sunken and requires the
list to carry it.
