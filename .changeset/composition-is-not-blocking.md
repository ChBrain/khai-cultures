---
---

**Retract the group-to-member edge.**

It shipped in #740 and moved `bolivia` from seventieth in the queue to first,
carrying 737 points from five groups. Every one of those points is a group's own
`no origin`, `no present` and unchained Triggers, which writing bolivia cannot
touch. A group composes its members and does not block on them. `dependencies`
returns empty until a unit can declare what it waits on.
