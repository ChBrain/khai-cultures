---
---

The house rules learn the two commands the house now has.

`npm run preflight` and `npm run skeleton` shipped in the last three pull
requests and were written down nowhere: not in `house_instructions.md`, not in
either tool file. An agent reading the brief - which is the whole point of the
brief - would not have known they exist.

That is the same drift these rules were just rewritten to end, committed by the
same hand that rewrote them, three pull requests later. A tool nobody is told
about is a tool nobody runs.
