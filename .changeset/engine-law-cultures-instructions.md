---
"@chbrain/khai-cultures": patch
---

Declare the engine law in the house instructions instance (cultures_instructions.md). It is the khai-engine-spine Prose baseline (the runtime register: everything is a Scene, played through Personas) with a single line added to its Knowledge chapter, wiring the language engine: language is a loop each persona runs per channel at its own width, rooted in process_using_language.md, where what is heard is not what is understood and the gap is the whole of it. The governance lane in khai-guard.config.json is widened to own the file. The Prose System modes are kept as bullets rather than H3 subsections, since engine-imposed H3 extensions validate only inside their engine package, not in a house instructions file. Non-content governance; the culture count is unchanged.
