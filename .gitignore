node_modules/
dist/
*.log
.DS_Store

# Subagent worktrees. Tooling state, never repository content: each is a full
# checkout of this repo that the agent runtime creates and removes on its own.
.claude/
